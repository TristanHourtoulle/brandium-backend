// Setup file for Jest tests
// Will be extended in Phase 7 with database connection

// Set test environment
process.env.NODE_ENV = 'test';

// Increase timeout for async operations
jest.setTimeout(10000);
